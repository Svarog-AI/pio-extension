# Architecture

## Patterns and Design Decisions

### Extension Point Architecture

pio is a **pi extension** — it registers with the pi coding agent framework via `src/index.ts`, which exports an `async` default function `(pi: ExtensionAPI) => void`. This function:
1. Registers discoverable skills (`resources_discover` event)
2. Wires shared infrastructure: `setupSessionInfrastructure()`, `setupValidation()`, `setupSessionGuard()`, `setupLoopEngine()`, `setupDirectTools()`
3. **Auto-discovers** AI-driven capabilities via `discoverCapabilities(__dirname)` which scans `src/capabilities/` for directories containing `config.ts`, then calls `registerCapability(pi, descriptor)` for each
4. Registers non-AI tools (init, delete-goal, list-goals, parent, create-issue, goal-from-issue) via `setupDirectTools(pi)` — consolidated in `src/direct-tools.ts`
5. Explicitly imports and registers state machines (e.g., `state-machines/pio-workflow-machine`) to ensure machines are available before the exit lifecycle (`runExitLifecycle`) dispatches transitions

Test fixtures (`test-*` prefixed directories) are automatically filtered out from the discovery loop.

**Sandbox capabilities** (e.g., `workflow-playground`) are auto-discovered like production capabilities — they do not use the `test-` prefix. They exercise loop engine features (write gates, bounded loops, do-while loop blocks, terminateWhen conditions, restricted writes) in a live sub-session without side effects. Unlike production capabilities, sandbox tools typically make `workspacePrefix` optional with a default path to an isolated sandbox directory (e.g., `goals/test-playground`). Their `additionalContext` is instructional — directing agents through test phases and expected observations.

### Skill Auto-Discovery

Skills are auto-discovered from the filesystem — no hardcoded registration list. The `setupSkills()` function in `src/index.ts` scans `SKILLS_DIR` at startup using `fs.readdirSync()`, filtering directories by `SKILL.md` existence. To add a new skill, create its directory and `SKILL.md` file under `src/skills/`; it will be registered automatically on next startup. If `SKILLS_DIR` doesn't exist or is unreadable, the scan silently produces an empty array rather than crashing.

**Active skills:** Only `pio-git` and `test-driven-development` remain in `src/skills/`. The pio-specific skills (`pio`, `pio-planning`, `pio-project-knowledge`, `pio-jira`, `grill-me`, `write-a-skill`) were moved to `src/skills.old/` to prevent auto-discovery — capabilities now declare needed skills via `WorkflowPhase.skills` and capability config `skills` fields, making filesystem discovery redundant for workflow-specific guidance.

### Capability Package Pattern

Each AI-driven capability is a **directory package** under `src/capabilities/<name>/` with structured component files:
1. **`config.ts`** — default exports `CapabilityPackageConfig` (session shape: validation, file protections, skills, frontmatterSchemas). Named export `register(pi)` registers tool + command with the pi API
2. **`role.md`** — Role description (prompt component)
3. **`workflow.ts`** — default exports `WorkflowPhase[]`, each phase can declare `skills: { mandatory?: string[], recommended?: ... }`
4. **`guidelines.md`** — Guidelines (prompt component)
5. **`callbacks.ts`** *(optional)* — Lifecycle callbacks: validation, file protection resolvers
6. **`schemas.ts`** *(optional)* — Capability-local TypeBox frontmatter schemas for output validation
7. **`config.test.ts`** — Colocated tests

Prompts are **compiled at runtime** by `prompt-compiler.ts` (`compilePrompt()`) from component files instead of reading single `.md` files. The old `src/prompts/` directory was removed.

**Non-AI capabilities** (init, delete-goal, list-goals, parent, create-issue, goal-from-issue) are consolidated in `src/direct-tools.ts` — they have no prompts, no sub-sessions, no validation hooks.

**Registration:** `discoverCapabilities()` auto-discovers directory packages; `registerCapability(pi, descriptor)` calls the `register(pi)` named export from each `config.ts`. No hardcoded per-capability imports in `index.ts`.

**Tool-only architecture (no command handlers):** All capability command handlers (`handleCreatePlan`, `handleEvolvePlan`, etc.) were removed. Capabilities register tools via `defineTool()` only — no `registerCommand()` calls remain in any capability `config.ts`. The removed imports (`ExtensionCommandContext`, `resolveCapabilityConfig`, `launchCapability` from command contexts) eliminated an entire class of code duplication between commands and tools. Users interact with pio exclusively through `pio_*` tools (agent-callable) or TUI-only commands (`/pio-next-task`, `/pio-parent`, etc.). Direct tool invocation is the canonical interface; state machine transitions provide params automatically.

**Project-scoped capabilities:** Capabilities that read/write `.pio/PROJECT/*.md` files (e.g., `project-context`, `finalize-goal`) use `projectRelative: true` on `MarkdownFileSpec` entries. This flag resolves paths from the global `pioRootDir` (`<cwd>/.pio/`) instead of the capability's workspace directory. Combined with the validation guard's `allowProjectWrites` opt-in, these capabilities can freely write to PROJECT files without workarounds.

### Sub-Session Lifecycle

The `capability-session.ts` module (renamed from `session-capability.ts`) orchestrates sub-sessions:
1. **Launch:** `launchCapability()` calls `ctx.newSession()` with a custom `pio-config` entry containing prompt, working directory, validation rules, and file protections
2. **Resources discover:** Config is read, prompts loaded, `prepareSession` hooks run, session name set. New-style capabilities (directory packages) use `compilePrompt()` to assemble prompts from component files; old-style falls back to reading `.md` files directly. Capabilities can define `prepareSession` callbacks (e.g., `execute-task`, `review-task`) that execute here — before `before_agent_start`. This allows runtime config enrichment: execute-task and review-task use `prepareSession` to read per-step skills from TASK.md frontmatter and merge them into the capability config via `mergeCapabilitySkills()` from `capability-utils.ts`.
2b. **Workspace directory injection:** `buildSessionInputsSection()` (`capability-session.ts`) always injects `Workspace directory: <absolute-path>` into the `--- SESSION INPUTS ---` system prompt section, regardless of whether contract inputs exist or resolve. This replaces the old enrichment-prefix approach where workspace dir was prepended to `additionalContext` during normalization — passthrough is now the only path (no enrichment, no transformation). State-machine messages include file-reading guidance (which files to read, where to write) and goal names where appropriate.
2c. **Additional context delivery:** When `config.additionalContext` is present, `before_agent_start` injects it as a system prompt section `--- ADDITIONAL CONTEXT ---` after `--- WORKFLOW EXECUTION ---`. The value flows through `sessionParams` → normalization → config and persists across restarts automatically. No default fallback — the field is purely optional; when absent, no section is injected.
3. **Before agent start:** `.pio/PROJECT/OVERVIEW.md` + compiled capability prompt are injected as a custom conversation message (preserves pi's default system prompt). Model switching occurs here if `~/.pi/pio-config.yaml` specifies per-capability models. Skill loading runs via `buildSkillLoadingSection()`: mandatory skills from the config are frontmatter-stripped and wrapped in `<skill>` XML tags; recommended skills listed as instructions. Global defaults (`pio`, `ask-user`) are always prepended.
4. **File protection:** The `tool_call` event handler enforces read-only files and write allowlists, with a default-deny policy for `.pio/` writes outside the session's own goal workspace
5. **Plan revision trigger:** During evolve-plan, if the specification writer detects significant divergence from the plan, it writes `REVISE_PLAN_NEEDED.md` at the workspace root (next to PLAN.md). The transition resolver checks for this file via `tryResolveOutput("revise-plan")` and routes to `revise-plan` instead of continuing normally. The unified workspace-root location handles both mid-plan revisions and completion-time revision requests (step n+1).
6. **Completion (automatic):** No agent action required — when traversal reaches the end of the workflow, the engine runs the synthesized terminal code phase (`__pio-exit`), which calls `runExitLifecycle()` (`src/runtime/exit-lifecycle.ts`): validate outputs, dispatch transitions via `dispatch()` on registered state machines (single/multiple/no matches), auto-enqueue the next task, apply declarative markers (APPROVED/REJECTED/COMPLETED/BLOCKED), run postExecute, then fileCleanup. The former `pio_mark_complete` tool (and its step-position guard) is removed — `__pio-exit` is the only exit path. On validation failure there is no automatic retry: the session pauses in ad-hoc mode (`Session validation failed: <msg>` appended to the "Workflow Paused" message while live), and a user `/continue` (or restart into paused mode) re-runs `__pio-exit`. There is no idempotency guard — re-traversal re-runs the lifecycle (duplicate transitions.json entries accepted)

### State Management — CapState + Contract Caching

- **`CapState`** (`capability-state.ts`) provides contract-backed lazy file access. Wraps a `CapabilityContract` with a base directory and params. Core accessors:
  - **`.input<T>(name)`** — looks up by `name` in `contract.inputs`, resolves placeholders, validates against schema
  - **`.output<T>(name)`** — looks up by `name` in `contract.outputs` via recursive traversal of the full `OutputEntry` tree (including inside OneOfGroup entries at arbitrary nesting depth)
  - **`.undeclared(path)`** — for marker files not in any contract (no placeholder resolution, no schema validation; passes explicit `projectRelative: false`)
  - **`.resolvePath(entry: MarkdownFileSpec)`** — unified path resolution method. All internal accessors (`input`, `output`) and external consumers flow through this single method. Handles `projectRelative` entries (resolves from global `pioRootDir`), placeholder resolution, workspace prefix injection, and `paramKey` resolution.
  - **Dynamic inputs via `paramKey`:** `MarkdownFileSpec` has an optional `paramKey?: string` field. When set, the resolver reads the file path from `params[paramKey]` at runtime — the static `file` value serves as fallback/label only. This enables capabilities like `quality-gate` to declare inputs whose paths come from state machine params (e.g., `{ name: "requirements", paramKey: "requirementsFile" }`). Validation in the CapState constructor enforces "at least one of file/paramKey"; resolution throws if both are absent.
  Entry maps store full `MarkdownFileSpec` references (not copies) — new fields on the spec are automatically available. Public `.contract` getter exposes the wrapped contract. Every `MarkdownFileSpec` requires a `name: string`. Duplicate names across inputs/outputs throw at construction **unless** they share the same name and file path — in that case, `registerOutputEntry()` silently deduplicates (first registration wins). This enables multiple OneOfGroups to reference the same entry (e.g., `"revise-plan"` → `REVISE_PLAN_NEEDED.md`) without conflict; downstream `tryResolveOutput("revise-plan")` resolves to a single correct file regardless of which group registered it first. Replaces the old `GoalState` (deleted).
- **Contract caching at startup:** `index.ts` calls `setDiscoveredContracts()` after `discoverCapabilities()` to build a name-to-contract map. `getCapState(capabilityName, baseDir, params?)` (in `state-machines/utils.ts`) provides synchronous lookup for resolve functions — no dynamic imports needed at runtime.
- **Resolve function pattern:** All resolve functions in `pio-workflow-machine.ts` accept minimal `{ workspaceDir: string }` context and call `getCapState()` with capability names to perform file reads. `workspaceDir` is already the resolved directory (includes workspace prefix). No intermediate context object (PioWorkflowContext removed). No cross-capability schema imports from capabilities — framework code imports capability CONTRACTs directly.

**Removed:** `GoalState` (`goal-state.ts`, deleted), `PioWorkflowContext` and `buildPioWorkflowContext()` (removed from `pio-workflow-machine.ts`). Dead filesystem utilities in `state-machines/utils.ts` (`isGoalComplete`, `findCurrentStepNumber`, `SimpleStepStatus`) were also removed — the module now houses only `setDiscoveredContracts` / `getCapState`.
- **Declarative state machine framework** (`state-machines.ts`) replaces the old imperative `switch`-based resolver (`state-machine.ts`, deleted). The framework is built on three generic types:
  - **`StateMachine<C>`:** Named config with `id`, `name`, `description`, and `edges: TransitionEdge<C>[]`
  - **`TransitionEdge<C>`:** Directed edge `{ from, to, resolve }` — each edge carries its own `resolve(context, params) → TransitionResult | undefined` that combines condition check + param computation. No separate `condition` field.
  - **`ResolverResult`:** `Omit<TransitionResult, "stateMachineId">` — the type resolvers return; `stateMachineId` is stripped from the resolver contract since dispatch auto-injects it. Includes optional `cleanup?: string[]` field: an array of input spec names from the *completing* capability's own contract. the exit lifecycle (`runExitLifecycle`) resolves these through the completing session's CapState and deletes the files, enabling declarative signal-file cleanup at transition boundaries (e.g., deleting COMPLETION_SUMMARY.md after quality-gate consumes it).
  - **`TransitionResult`:** `{ capability, stateMachineId, params? }` — identifies which machine produced the result for multi-machine tracking
- **Dispatch mechanism:** `dispatch(machine, currentNode, context, params)` iterates outgoing edges and collects all non-undefined results. No switch statements. When `machine` is `undefined`, iterates all registered machines (multi-machine dispatch). Each resolve call is wrapped in try/catch — one bad machine doesn't block others.
- **Registration:** `registerMachine(machine)` / `unregisterMachine(id)` manage the machine registry. Machines are registered explicitly via named setup functions called from `index.ts` (e.g., `setupPioWorkflowMachine()`) — no auto-registration at module load.
- **Machine lookup:** `_machinesById` (`Map<string, StateMachine<unknown>>`) is the single internal storage structure. Public API: `getMachine(id)` for O(1) lookup by ID, `getRegisteredMachines()` returns a snapshot array. The old `_registeredMachines` array was removed.
- **Multi-machine dispatch:** When `machine` is `undefined`, `dispatch()` calls `getRegisteredMachines()` (not a private variable). For explicit dispatch, the exit lifecycle (`runtime/exit-lifecycle.ts`) reads `stateMachineId` from session params, looks up the machine via `getMachine()`, and passes it to `dispatch(machine, ...)`. Falls back to `dispatch(undefined, ...)` if ID is absent or unknown — preserving backward compatibility.
- **Dispatch auto-injects `stateMachineId`:** Every returned `TransitionResult` carries `stateMachineId: m.id`, injected by `dispatch()` via `{ ...result, stateMachineId: m.id }`. Resolvers return `ResolverResult` (which omits `stateMachineId`) — the framework guarantees the ID, not individual resolvers. This eliminated 14 manual `stateMachineId` assignments in `pio-workflow-machine.ts` resolver returns.
- **`stateMachineId` propagation:** Included at top level in enqueued task params (spread after `_sessionContext`) so it persists through sub-session launches. Matches the existing `stepNumber` propagation pattern.
- **Enriched audit logging:** `recordTransition()` accepts an optional fourth parameter (`actualParams`) to record enriched params (with `stateMachineId`, `_sessionContext`, explicit `stepNumber`) instead of raw edge resolver params. When omitted, falls back to `toResult.params` for backward compatibility. The exit lifecycle (`runExitLifecycle`) passes the same `enrichedParams` object to both `enqueueTask()` and `recordTransition()`, ensuring transitions.json mirrors exactly what was dispatched.
- **Workflow machine** (`state-machines/pio-workflow-machine.ts`) — exports 14 resolve functions (one per edge) with explicit naming convention: `resolve<From>To<To>` (e.g., `resolveCreateGoalToCreatePlan`). Includes blocked-step transitions: `execute-task → evolve-plan` (blocked status) and extended `review-task → evolve-plan` (BLOCKED decision). The quality-gate capability added two outgoing edges (`quality-gate → finalize-goal`, `quality-gate → revise-plan`) and one incoming edge (`evolve-plan → quality-gate`), replacing the old direct `evolve-plan → finalize-goal` edge. Ported from old `transition*` functions in deleted `state-machine.ts`. No longer exports `recordTransition()` — that function lives in `state-machines.ts`. Resolver return types changed to `ResolverResult` (no manual `stateMachineId`) after dispatch auto-injection was added.
- **Edge priority:** Edges are evaluated in array order. Conditional edges guard against higher-priority conditions to prevent duplicate results (e.g., evolve-plan → execute-task returns `undefined` if revision needed or subgoal present). Higher-priority conditions must appear earlier in the edges array.
- **Per-goal task queues** (`queues.ts`) use single-slot files at `.pio/session-queue/task-{key}.json` — one pending task per goal. For flat goals, `key` is the goal name basename. For nested subgoals, `deriveQueueKey(goalDir, cwd)` produces hierarchical keys (e.g., `parent__S03__nested`) using `__` as delimiter. On completion, the exit lifecycle (`runExitLifecycle`) uses the transition's adjusted `params.goalName` to determine which queue slot to enqueue into, enabling parent queue slot restoration when a subgoal completes.
- **Manual override:** `pio_transition` tool and `/pio-transition` command (in `direct-tools.ts`) allow users to manually select transitions — useful when multiple edges match or the auto-resolution is undesirable.

### Nested Subgoals

Plan steps declared with `complexity: "subgoal"` in the PLAN.md frontmatter `steps` array spawn child goal workspaces under `S{NN}/subgoals/<name>/`. These subgoals run through the full pio lifecycle recursively:

1. **Spawning:** `transitionEvolvePlan` detects `complexity === "subgoal"` via `state.steps()[n].getMetadata()` and routes to `create-goal` with parent context (`parentGoalName`, `parentStepNumber`, `workspaceDir`) and an `additionalContext` containing a relative path to the parent step's TASK.md
2. **Directory structure:** Subgoal workspace lives at `.pio/goals/<parent>/S{NN}/subgoals/<name>/` — nested inside the parent step folder, not at the top-level goals directory
3. **Path resolution:** `resolveGoalDir(cwd, name, parentStepDir?)` supports an optional `parentStepDir` for nested subgoal paths; backward compatible with flat goals
4. **Completion propagation:** `transitionFinalizeGoal` routes subgoals back to the parent's `evolve-plan` (restoring the parent queue slot). Top-level goals return `undefined` (terminal)
5. **Detection mechanism:** Frontmatter-only — no regex heading parsing or `[subgoal]` body annotations. The `steps` array in PLAN.md frontmatter is the single source of truth
6. **Universal TASK.md:** Both `execute-task` and subgoal `create-goal` read TASK.md. Evolve-plan produces only TASK.md; tests are derived at execute-time by the executor using the `test-driven-development` skill
7. **Backward compatible:** Flat goals without subgoal metadata function identically — `planMetadata()` returns null for old plans, `getMetadata()` defaults `complexity` to `"task"`

### Skill Injection Architecture

Skills are loaded dynamically at session startup, replacing the old static `_skill-loading.md` approach. Resolution order (highest priority first):

1. **Per-step skills** — Declared in TASK.md YAML frontmatter (`skills.mandatory`, `skills.recommended`) or in capability `workflow.ts` step declarations. Read during `prepareSession`. Merged with base config skills via `mergeCapabilitySkills()` from `capability-utils.ts` (Set-based dedup for mandatory, Map-based first-seen-wins for recommended).
2. **Base capability skills** — Declared in each capability's `CapabilityPackageConfig.skills` field. Propagated through `resolveCapabilityConfig()` into runtime `CapabilityConfig`.
3. **Global mandatory skills** — `pio` and `ask-user` are always injected by `buildSkillLoadingSection()`, regardless of capability config.

Mandatory skills are force-injected (content read from disk, frontmatter stripped, wrapped in `<skill>` XML tags). Recommended skills are listed as loading instructions (LLM decides whether to load based on condition).

### Key Design Decisions

1. **Markdown-first workflow:** GOAL.md, PLAN.md, TASK.md, TEST.md, REVIEW.md are the authoritative artifacts. No database or structured state
2. **Completion signal:** `COMPLETION_SUMMARY.md` replaces the empty `COMPLETED` marker at the goal level. Written by evolve-plan with YAML frontmatter (`status: "complete"`) and a markdown body explaining why the goal is complete. Enforced via two `OneOfGroup` contract entries:
   - **Group 1 (steps ≤ n):** Mutual exclusion between inner AND-group `[TASK.md, DECISIONS.md]` and `REVISE_PLAN_NEEDED.md` — enables mid-plan revision requests without validation failure.
   - **Group 2 (step > n):** Mutual exclusion between `COMPLETION_SUMMARY.md` and `REVISE_PLAN_NEEDED.md` — enables evolve-plan to request a plan revision instead of completing.
   Both groups use the entry name `"revise-plan"`; CapState dedup ensures a single registration in `outputNames`, so `tryResolveOutput("revise-plan")` works regardless of which group is active.
3. **File markers for step state:** APPROVED, REJECTED, BLOCKED — empty files as state indicators (checked by marker priority)
4. **Declarative markers (`contract.markers`):** Capabilities declare marker behavior via the optional `markers?: MarkerDeclaration[]` field on `CapabilityContract`. Each `MarkerDeclaration` specifies `outputFile` (matching a contract output name), `field` (frontmatter key), and `values` (record mapping frontmatter value → marker filename, e.g., `{ completed: "COMPLETED", blocked: "BLOCKED" }`). The framework marker engine (`applyMarkers`, colocated with `cleanupMarkers` in `guards/mark-complete.ts`) runs inside the exit lifecycle (`runExitLifecycle`, step 4a) — reads frontmatter via CapState, creates the matched marker file, and deletes stale markers from the same declaration. This replaces per-capability `postExecute` callbacks for marker creation. Both execute-task and review-task have been converted to declarative markers. revise-plan retains imperative logic (`cleanupIncompleteSteps`) as its cleanup complexity exceeds the declarative model.
5. **Marker auto-cleanup at session start:** When a capability declares `markers[]`, `cleanupMarkers` (colocated in `guards/mark-complete.ts`) runs during `resources_discover` in `capability-session.ts`, before capability-specific `prepareSession`. It collects all filenames from all `values` mappings (deduplicated via `Set`) and deletes each with `force: true`. This eliminates per-capability boilerplate for deleting stale markers at startup.
6. **REJECTED → delete COMPLETED behavior shift:** Previously, review-task's `applyReviewDecision()` explicitly deleted the COMPLETED marker on rejection. Now, framework auto-cleanup handles this — execute-task's `cleanupMarkers` deletes COMPLETED at its own session start when re-executed after rejection. Between rejection and re-execution, both COMPLETED and REJECTED may coexist on disk — acceptable because state machine routing reads REVIEW.md frontmatter directly, not marker files.
3. **Capability package system:** AI-driven capabilities are directory packages (`src/capabilities/<name>/`) with component files: `config.ts` (default export + `register(pi)`), `role.md`, `workflow.ts`, `guidelines.md`, optional `callbacks.ts` and `schemas.ts`. Prompts compiled at runtime by `prompt-compiler.ts` from component files
4. **Auto-discovery:** `discoverCapabilities()` scans `src/capabilities/` for directory packages containing `config.ts`; `registerCapability(pi, descriptor)` handles registration — no hardcoded imports in `index.ts`
5. **Direct tools consolidation:** Non-AI capabilities (init, delete-goal, list-goals, parent, create-issue, goal-from-issue, pio_transition) consolidated in `src/direct-tools.ts` — single module for simple tool/command registrations
6. **Mandatory contracts:** Every capability declares a `CONTRACT: CapabilityContract` at module level (mandatory on both `CapabilityConfig` and `CapabilityPackageConfig`). Validation functions (`validateOutputs`, `validateFrontmatter`, `validateInputs`) accept only `CapabilityContract` — old types (`FrontmatterSchemaDeclaration`, `ValidationRule`, `InputValidationSpec`) deleted. The contract is consumed by callbacks, CapState, tests, and the auto-discovery cache.
7. **Recursive output tree (`OutputEntry`):** `contract.outputs[]` accepts a recursive triple union: `MarkdownFileSpec | OneOfGroup | OutputEntry[]`. Bare arrays act as implicit AND-groups (all contained outputs required). `OneOfGroup` is a class with literal `kind = "one-of" as const`, constructed via `new OneOfGroup([...])`. Group-level `requiredWhen` predicates control the entire group. Type guards (`isMarkdownFileSpec`, `isArrayOutput`, `isOneOfGroup`) enable exhaustive narrowing for recursive traversal. Both validation (`validateOutputs`) and map building (`CapState.buildAllMaps()`) traverse the full tree recursively.
7. **Automated input validation:** `launchCapability()` performs contract-based input validation automatically — eliminated redundant `validateInputs()` calls from ~8 call sites across capability callbacks and config files
8. **Input frontmatter validation:** `validateInputs()` validates frontmatter schemas when declared on input entries (e.g., evolve-plan imports PLAN.md with `PLAN_FRONTMATTER_SCHEMA`). Output validation (`validateOutputs()`) also validates frontmatter — standalone `validateFrontmatter()` preserved as export but called only internally.
9. **CapState-aware output predicates:** `MarkdownFileSpec.requiredWhen` signature is `(params?, capState?) => boolean`. Predicates can read contract inputs (e.g., PLAN.md frontmatter via `capState.input("plan").read()`) to make output requirements dynamic. Example: evolve-plan uses a `OneOfGroup` with group-level `requiredWhen` (active when `stepNumber > totalSteps`) to require either `COMPLETION_SUMMARY.md` or `REVISE_PLAN_NEEDED.md` — mutual exclusion enforced at validation time.
9b. **Dynamic revision context:** revise-plan accepts a dynamic input (`{ name: "revision-context", paramKey: "revisionContextFile" }`) instead of hardcoding `REVISE_PLAN_NEEDED.md`. This allows both the old evolve-plan path (passing `REVISE_PLAN_NEEDED.md`) and the new quality-gate path (passing `QUALITY_GATE.md`) to feed revision context through the same input slot. The resolver-declared cleanup (`cleanup: ["revision-context"]` on `revise-plan → evolve-plan`) handles deletion of the context file, replacing the old `postExecute: cleanupRevisionRequest` callback.
10. **Declarative state machines:** Transition resolution uses the generic `StateMachine<C>` framework (`state-machines.ts`) — edges with `resolve` functions replace imperative `switch` statements. Machine registry backed by `_machinesById` Map with O(1) lookup via `getMachine(id)`. Machines register explicitly via named setup functions from `index.ts`. The exit lifecycle (`__pio-exit` → `runExitLifecycle`) dispatches against the correct machine by reading `stateMachineId` from session params (multi-machine aware). Fallback to `dispatch(undefined, ...)` for backward compatibility. 
9. **Runtime Loop Engine:** Replaced prompt-based step nudging with a bounded iteration loop (`src/runtime/loop-engine.ts`). Each workflow step executes as one or more *agent runs* (iterations). The engine enforces:
- **Iteration bounds:** `minIterations` (per-step, default 1) guarantees minimum agent runs; `maxIterations` (global config > built-in default of 15) is a hard safety cap. Resolution: per-step > global `~/.pi/pio-config.yaml` loop block > built-in default.
- **Termination conditions:** After each iteration (once min reached), evaluates `terminateWhen` against `StepState` (`filesWritten[]`, `filesEdited[]`, `askUserCalled`). Supports callback functions; OR logic — any passing condition advances the step.
- **Turn-level tracking:** Dead-turn detection and refinement-loop nudge via `turn_end` handler.
- **Steering messages:** Delivered via CustomMessage injection at `turn_end` using step-specific `loopMessage`. System prompt is process-aware only (how to work, not what to produce); all step content flows through CustomMessage — KV cache safe across agent runs.
- **Follow-up messages:** Inter-iteration transitions delivered via `sendUserMessage(..., { deliverAs: "followUp" })` at `agent_end`. Skipped on user abort (`stopReason === "aborted"`), error, or mark-complete termination (detected via `markCompleteCalled` in shared session state).
- **Ad-hoc mode:** User messages mid-execution pause the step for free conversation. `/continue` command exits ad-hoc mode and resumes the workflow from the current phase/iteration (iteration preserved, unlike the former `/return` which reset iteration to 1).
- **Write gate (restricted-by-default):** Each capability workflow declares `write: [output-names]` on output-producing steps. Steps without `write` cannot produce contract outputs (source code, tests, docs unaffected). The gate checks against resolved absolute paths during `tool_call` events.
- **Per-phase project write gate:** `WorkflowPhase.allowProjectWrites?: boolean` (defaults to `false`) extends the write gate to non-contract project files. In the same `tool_call` loop, after `/tmp/` skip and contract-output handling, writes under the project root are blocked unless the active phase opts in with `allowProjectWrites: true`. The project root is stored in-memory-only as `PioSessionState.projectRoot`, set once at `resources_discover` via `path.resolve(ctx.cwd ?? process.cwd())`; an undetermined root fails safe by blocking (not skipping). There is no `.pio/` exclusion — capability-level `validation.ts` governs `.pio/` paths separately. The old `if (!allOutputs) continue;` guard ("allow everything when no contract outputs known") was removed, so sessions with zero declared outputs are fully restricted by default. This made the default stricter: non-contract project writes that previously passed through unintentionally now require explicit per-phase opt-in (goal: phase-project-write-gate).
- **State persistence:** Loop engine state (`currentPhase`, `currentIteration`, `isAdHocInput`, and writable runtime variables) is persisted to disk as JSON files keyed by session ID. Restore on `resources_discover`; save on `agent_end` (loop replay + phase advancement), `input` handler, `/continue` command, and `session_shutdown` (flush on reload/quit). Enabled via `src/runtime/state-persistence.ts`. Per-iteration tracking (`filesWritten`, `askUserCalled`) is intentionally NOT persisted — it resets safely each iteration.
- **Template interpolation:** `buildPhaseInstructions()` calls `store.interpolate()` on both `phase.instructions` and `phase.loopMessage`, resolving `${name}` placeholders from session params + runtime variables. Covers initial injection (`before_agent_start`) AND follow-up messages (`agent_end`).
- **Variable-defining phases:** `WorkflowPhase.kind: 'variable-definition'` with `variables?: PhaseVariable[]` triggers a fixed template (not freeform instructions) listing LLM-driven vars with prompt text, static pre-set vars, and computed auto-computed vars.
- **Code phases (`kind: "code"`):** Programmatic phase kind — `run(ctx: CodeStepContext)` executes arbitrary TypeScript instead of an LLM turn. Treated as programmatic by `isProgrammatic()`; executed inline (awaited) during traversal so a code step's effects (store writes, log entries) are visible to later phases in the same traversal. `CodeStepContext` is a minimal one-member seam — exactly `{ readonly state: PioSessionState }` (pinned by a compile-time test) — callbacks reach everything via `ctx.state` (variables through `state.store`, contract files through `state.capState`). No control-flow API (no goToPhase/abort): routing = variables + declared `branch:if`/`branch:switch` phases after the code step. Pairing is enforced at `PhaseManager` construction (`kind: "code"` ⇔ function `run`; a non-code phase carrying `run` throws `TypeError`). A throwing `run()` is warn-and-continue: caught, `console.warn`ed, recorded as one entry in in-memory-only `state.programmaticLog` (`detail` carries the error message on throw), and surfaced to the next LLM turn via the `## Programmatic activity since your last turn` section rendered (and cleared) by `buildPhaseInstructions()`.
- **Automatic session exit (`__pio-exit` terminal phase):** At `resources_discover`, the engine appends exactly one synthesized code phase — `{ id: "__pio-exit", title: "Exit lifecycle (automatic)", kind: "code", run: exitLifecycleRun }` — after the declared phases, so workflow end always runs the exit lifecycle without an agent tool call. Skipped for zero-phase capabilities (synthesis would fire exit on turn one). No id-based special-casing — `__pio-exit` is handled by the generic code branch; `/goto __pio-exit` is intentionally reachable as a manual re-run. The wrapper (`exitLifecycleRun`) resolves the cached capability config via `getCurrentCapabilityConfig()`, then runs `runExitLifecycle(config)` (`src/runtime/exit-lifecycle.ts`): no config → `skipped`; success → `exitOutcome: "success"` + `markCompleteCalled: true` (+ clears stale failure message, logs the enqueue notification); returned failure → `exitOutcome: "failed"` + `exitFailureMessage`, ad-hoc pause armed (`isAdHocInput: true`, `currentPhaseId = lastLlmPhaseId ?? currentPhaseId`) with NO automatic retry — the user fixes and `/continue`s (or restarts into paused mode), re-running `__pio-exit`; throw → caught, `skipped` + `markCompleteCalled` (never blocks session end). No idempotency guard: any re-traversal re-runs the lifecycle (accepted worst case = duplicate transitions.json audit entry + postExecute re-run, matching the removed tool's re-invocation tolerance). All exit state (`lastLlmPhaseId`, `exitOutcome`, `exitFailureMessage`) is in-memory only — excluded from the persistence projection.
- **Phase navigation by ID (`PhaseManager`):** `src/runtime/phase-manager.ts` — registry abstraction mapping phase IDs to phase objects. Replaces direct array indexing (`phasesList[currentPhase - 1]`) with `getPhase(id)`. `resolveNext(currentId, state?)` returns the next-in-order phase ID — sequential via `_routing` map for normal phases; conditional branching (IF/ELSE) for `branch:if` and `branch:switch` kinds. Uses two routing maps: `_routing` (linear successor links) and `_conditionalRouting` (branch arm first-phase IDs, plus `LoopBackRouting` repeat/exit entries keyed by synthetic loop-end node ids). Created during `resources_discover`, stored on `PioSessionState.phaseManager`, never persisted. Tree flattening via depth-first walk — nested branch arms and loop bodies are flattened at construction time, synthesizing the branch-end/loop-end merge nodes; all arm-first values are concrete strings.
- **`currentPhaseId` in session state:** String field on `PioSessionState` set alongside the numeric `currentPhase` during transitions. Primary lookup key going forward. Persisted in state-persistence JSON; required (old files without it are rejected by validation).
- **`/goto <phase-id>` command:** Registered via `pi.registerCommand("goto", ...)` for manual phase jumps. Validates against PhaseManager, resets iteration to 1, clears tracking, persists, triggers follow-up. Guarded by `isIdle()` — blocked during active agent runs with error notification. Supports autocomplete via `getArgumentCompletions` callback (case-insensitive prefix match on phase IDs); completions and the "Available phases" error list exclude synthetic merge nodes (loop block ids remain listed — `/goto <loop-id>` lands at `body[0]`).
- **`/continue` command:** Resumes from the current phase/iteration, preserving `currentIteration` (unlike the former `/return` which reset to 1). Uses `advancePhase` with PhaseManager's `resolveNext` for ID-based advancement.
- **terminateWhen AND logic (breaking change):** All conditions must return true to advance. Any condition returning false or throwing forces a loop replay. Previously used OR logic.
- **loopWhile mechanism:** `WorkflowPhase.loopWhile?: LoopWhileCondition[]` — loop continuation with OR semantics (any passing callback keeps looping). Unified callback model: auto var completeness is generated as a default callback for variable-defining phases alongside user-defined conditions.
- **Conditional branching:** `branch:if` and `branch:switch` phase kinds enable non-linear workflows. Authors declare nested branch trees in `workflow.ts`; PhaseManager flattens depth-first into a flat registry with two routing maps (`_routing` for linear successors, `_conditionalRouting` for branch arm destinations). Branch point phases are programmatic — they execute inline via `isProgrammatic()` returning `true` for `kind.startsWith("branch:")`. Standard phases inside arm children DO trigger agent turns. Supports both callback conditions (`(state) => value`) and `$varName` string form (for `branch:switch`). All arm-first values are concrete strings; multi-phase arms chain sequentially (child-1 → child-2). **Single-exit convergence:** every arm — including empty/absent ones — converges on the branch's synthetic branch-end merge node, which routes to the successor; there is no direct arm-tail→successor link (see *Synthetic merge nodes* below).
  Authoring example:
  ```ts
  { id: "check", kind: "branch:if",
    condition: (state) => state.store?.get("needsReview") === true,
    then: [{ id: "review", title: "Peer review" }],
    else: [{ id: "skip", title: "Skip review" }] }
  ```
- **Do-while loop block (`kind: "loop"`):** Multi-phase repeating unit — `body?: WorkflowPhase[]` plus optional `repeatWhile?: (state) => boolean | unknown` (same callback shape as `condition`: truthiness evaluated, throwing → warn + fail-safe exit; omitted → always repeat). Do-while semantics: the container links unconditionally to `body[0]` (≥1 full pass guaranteed, condition never pre-checked); `repeatWhile` is evaluated at the end of each full body pass. `maxIterations` on a loop counts **full body passes** and restricts repeats only — repeat allowed iff `passes + 1 < resolvedMax` (resolved per evaluation via `resolveMaxIterations`; cap check precedes condition evaluation; cap exit silent; missing state → warn + exit). Pass counters live in `PioSessionState.loopPasses: Record<string, number>` keyed by block id (unique workflow-wide) — required field, in-memory only (restart resets counts), accumulating across re-entries with no per-entry reset. Repeat/exit data (`LoopBackRouting` added to the `BranchRouting` union, structurally discriminated — runtime keys on `"loopTarget" in routing`) is installed on the synthetic loop-end node, **not** the body tail — distinct ids per loop fix nested-loop last-wins Map collisions. Loop containers are programmatic (`isProgrammatic()` → true; no agent turn); all-programmatic bodies execute inline and terminate at the cap within a single `advancePhase()` call.
- **Synthetic merge nodes (single-exit model):** PhaseManager flattening synthesizes one no-op programmatic merge phase per control structure — branch-end (`__branch-end-<id>`; every arm of the branch, including empty ones, converges on it before the successor) and loop-end (`__loop-end-<id>`; carries the loop's `LoopBackRouting` repeat/exit decision). Shape: `{ id, title: id, kind: "code", run: no-op, synthetic: true }`; ids reserved (incrementing numeric-suffix collision guard against the registry; no duplicate detection added) before arm/body flattening and registered after — so they never become `_firstPhaseId`, and `listIds()` DFS order places each merge node after all its descendants. User-facing contract: merge nodes produce no "Programmatic activity" log entries (`executePhase` suppresses on the `synthetic` flag, never id prefix), never appear in `/goto` completions or the "Available phases" error (engine-local `listUserFacingPhaseIds()` filters on `phase?.synthetic`, null-safe), and don't count toward `totalPhases`. Loop block ids are NOT synthetic — they stay in `/goto` surfaces, and `/goto <loop-id>` lands at `body[0]` (documented behavior).

10. **Session Variable System:** `SessionVariableStore` (`src/runtime/session-store.ts`) enables dynamic workflow messaging and data-driven termination via `${name}` template interpolation. Two-layer design:
- **Read-only layer:** Session params frozen at startup in `resources_discover`. Agent cannot modify these via `setVar`.
- **Writable layer:** Runtime variables set by the agent with explicit types (`'string' | 'number' | 'boolean' | 'array' | 'object'`). Writable vars can shadow params of the same name.
- **Store ownership:** Lives on `PioSessionState` as `store?: SessionVariableStore | null`, accessed via `getState().store`. No module-level singleton. Type-only import in `session-state.ts` avoids circular deps.
- **Type enforcement & coercion:** Store uses strict string equality (`===`) for type comparison. A `coerceValue()` helper at the tool boundary (`setVarTool.execute`) coerces values to declared types before calling `store.set()`. Boolean coercion uses a closed truthy set (`true`, `"true"`, `"1"`, `"yes"`); NaN is rejected explicitly.
- **Value interpolation on set:** String values are interpolated (placeholders resolved) before storage — no cascading re-resolution on subsequent reads.
- **Phase gating for `setVar`:** Only allowed during variable-defining phases (`phase.kind === 'variable-definition'`). Returns error text in all other phases. All tools gate on `getState().isActive` and check store null safety.
- **Three agent-facing tools:** `setVar`, `getVar`, `listVars` defined via `defineTool()` with TypeBox schemas. Registered by `setupSessionVariables(pi)` at end of `setupLoopEngine()`.
- **Variable kinds:** `PhaseVariable.kind: 'static' | 'llm' | 'computed'`. Static vars set immediately before agent turn (overwritten on replay). LLM vars set by agent via `setVar` with prompt text. Computed vars run callbacks during phase advancement using previous phase's iteration data.
- **Validation gate:** Before termination evaluation, all declared variables must be defined. Undefined vars trigger a loop replay with follow-up listing missing variables. On max iterations, advances with warning.
- **Persistence:** Writable vars persisted as `{ value, type }` per variable via `state-persistence.ts`. Read-only params never written to disk — re-frozen from config on reload. Restore order: declare (type) then set (value) for type enforcement survival.
11. **Abort detection in session guards:** Both `step-nudging.ts` (`turn_end`) and `session-guard.ts` (`turn_end` + `agent_end`) detect user aborts via `stopReason` on event messages instead of `ctx.signal?.aborted` (unreliable — `activeRun` is cleared before events fire). The `turn_end` handler in `session-guard.ts` returns early on abort to skip turn counting and recovery prompts; `agent_end` checks the last message's `stopReason` to suppress completion warnings.
12. **Prompt compilation:** `compilePrompt()` reads component files (`role.md`, `workflow.ts`, `guidelines.md`) and assembles the final prompt — replaces monolithic `.md` prompts (old `src/prompts/` directory removed)
13. **Dynamic capability loading:** `resolveCapabilityConfig()` uses dynamic imports to load capability modules at runtime
14. **Callback-based config:** Validation rules and file protections can be static arrays or callbacks `(workspaceDir, params) => T` for step-dependent configuration
15. **No transpilation:** Runs as raw TypeScript ESM via pi's runtime — `tsconfig.json` is for type checking only
16. **Dual-tree repository layout (standalone pio executable):** Goal `pio-r1-probe-sdk-session` (R1) added a second in-repo project `pio/` (sibling of `src/`) — a self-contained npm package shipping the `pio` executable (`pio run <capability>`, currently only `probe`). Binding invariant: purely additive — `git diff experimental --name-only` lists exclusively `pio/` paths plus the recorded sanctioned exception from goal `capability-base`: the two root tooling files (`package.json`/`package-lock.json`) exact-pin `@biomejs/biome` 2.5.13 for cross-tree formatter parity (owner-directed; the `pi.extensions` entry itself stays untouched). Root extension (TUI-loaded via `package.json`'s `pi.extensions`) and executable are separate projects with independent manifests, lockfiles, tsconfigs, and vitest configs; shared formatting comes from the single root `biome.json` via upward walk-up (no duplicated biome config). **Module boundary:** the `pio/` package CANNOT import root-tree modules (separate project, different loaders) — any shape needed by both is re-declared locally inside `pio/` (e.g., `MarkdownFileSpec` in `capability/contract.ts` as a shape-compatible subset).
17. **Self-contained artifact / owned pi runtime:** `pio/` exact-pins `@earendil-works/pi-coding-agent` (0.85.1) as a *dependency* — the bundled pi is pio's own runtime, tracking upstream independently of whatever pi the host runs (root devDependency ^0.74.0 and the host global install stay decoupled). The pin is the outcome of a documented seam re-verification checklist (root export shape, `InteractiveMode(runtime, options)` ctor + `stop()`, `SessionManager.create` standard sessions-dir default, `CreateAgentSessionRuntimeFactory` contract incl. the `{ ...created, services, diagnostics }` result shape, pi CLI `main.js` mirror points, disk-backed model/thinking defaults); installed dist wins over stale GOAL/plan citations; discrepancies feed the re-verification record rather than blocking. Never silently downgrade; evidence lives in commit messages.
18. **Zero-build execution model (pio/):** `pio/bin/pio` is plain ESM JS; all `pio/` TypeScript executes via Node's native type stripping (floor Node ≥ 23.6, documented not enforced). `erasableSyntaxOnly: true` makes tsc reject non-strippable syntax at check time; `allowImportingTsExtensions: true` + explicit `.ts` import specifiers keep sources directly loadable by Node.
19. **Probe is a verification vehicle, not a capability:** `probe` is a flat top-level module (`pio/src/probe.ts`) deliberately kept out of any capabilities folder — no contract/body/composition. Quality-gate ruling (2026-09-16): probe has no future beyond proving the SDK/TUI/transcript plumbing; retirement is post-R1 scope. The durable assets are `pio/src/session.ts` (SDK session wiring, template for later capability bases) and the pinned packaging.
20. **Single-namespace sandbox launch path (R2, goal `pio-r2-sandbox-environment`):** `pio run <capability>` executes HOST-SIDE ONLY (the in-namespace top process knows nothing about sandboxes): strict gate order (capability → TTY fast-fail → anti-nesting → constructive bwrap pre-flight classified as absent / version-below-minimum (< 0.12.0) / setuid-binary / userns-denied / mount-rejected / usable — typed refusals NAME THE FAILING LAYER; fail-loud, never a silent unsandboxed run) → ensure engagement layout + ensure `<stateRoot>/.pi` (mkdir-only, first use) → `renderProfile` (ONE self-contained profile — ordered existence-checked mounts where later bind overlays earlier (ordering is security), cwd rw-bound LAST, NO merging ever — the profile IS the entire configuration) → `buildArgv` (pure, exact pinned token order) → `writeProfileFile` (retained `profile.json`; retained post-finish — absence is the feature) → mount-list printout pre-spawn (transparency layer — for external capabilities the printout IS the security review) → `superviseSpawn` (stdio inherit — no pty anywhere; host SIGTERM forward + await; exit map 0→0 / 130→130 / other→1; natural drain). Exactly ONE bwrap per top-level run; children are plain processes INSIDE the namespace (never nested bwrap); composition = logical frames, not extra profiles (no per-callee `profile.json`, ever). Displayed = retained = executed: one `SandboxProfile` value feeds the printout, the retained document, and the spawn-time argv (structured profile persisted; argv re-derived deterministically by `buildArgv`). Modules: `pio/src/sandbox/` (profile, render, fsview, profile-serializer, layout, launcher, run) with hermetic descriptor-table suites; the argv builder/serializer is generic over profile CONTENTS (new env/mount members flow through mechanically, never via serializer change).
21. **Isolated pio-owned pi instance (R2 quality-gate ruling R1–R5, 2026-09-22):** the bubble's pi operates on a FULLY ISOLATED, pio-owned pi configuration at `<stateRoot>/.pi` (stateRoot = `~/.pio`, follows `PIO_STATE_DIR`) — GLOBAL (one per machine/user; NOT per-engagement, NOT per-project), persistent across sessions (steady-state home of operator customizations, added in-place going forward), WHOLE TREE rw via ONE named-consumer mount `[rw] <stateRoot>/.pi` (the sole exception to the ro-global + own-slot-rw ruling), EMPTY-START pristine bootstrap (first-use creation is mkdir-only — ZERO seeding/copy machinery; "what's the point of an isolated environment if we copy from the host pi?"). Mechanism: the `PI_CODING_AGENT_DIR=<stateRoot>/.pi/agent` env pair (fixed emission order HOME → PATH → PI_SANDBOX → PI_CODING_AGENT_DIR; unconditional assignment; `HOME` stays the real home) relocates the COMPLETE pi configuration — measured on the pinned 0.85.1 dist: `getAgentDir()` returns the env value verbatim and ALL config/state paths derive from it (gapless). Host `~/.pi` is DECOUPLED — no bind in any mode (the former fully-ro default was retired: it was the EROFS defect condition — pi 0.85.1's auth storage acquires a lock file WRITE even for pure reads). Credentials/models provisioned in-place inside the isolated tree (`/login`; before the first provisioning `No API key found` is EXPECTED bootstrap state, distinct from the EROFS failure class). Risk lines: host key rotation does NOT propagate BY DESIGN; future pi-pin bumps may owe a `<stateRoot>/.pi` reset/migration consideration (the owned tree accumulates version-specific artifact shapes); `~/.pi/pio-config.yaml` REMAINS HOST-SIDE (the bundled instance never sees it; the pio extension is NOT loaded inside the namespace). Constraint-5 culture: anything the ruling removed (a host-`~/.pi` bind in any mode, any seeding path) may return ONLY via a new explicit owner decision.
22. **Engagement state layout under the global state root (R2):** engagements live at `<stateRoot>/projects/<projectKey>/engagements/<engagementId>/` — created on first use (id = fixed-width compact UTC ms + 8-hex suffix); the dir is the complete audit trail (retained `profile.json`, transcripts under `.sessions/top/` — the top session uses the FIXED NAME `top` — later child-session dirs nest under `.sessions/<childId>/`, plus the terminal `status.json` written by the capability path, see #24). Project key = slugified LAUNCH CWD (pure string derivation, no git/subprocess — owner ruling supersedes spec §7). Mount policy inside the bubble: state root bound RO for ALL sessions (siblings readable, not invisible — owner ruling), own project slot RW-overlaid on top, cwd rw-bound LAST (blast-radius lever), plus the named-consumer rw `.pi` mount; host `~/.pi` unbound. Do not conflate with the dev-repo legacy `.pio/` (pio's own goal/workflow state during transition) or `~/.pi/pio/` (loop-engine state).
23. **Capability authoring surface (goal `capability-base`):** new module family `pio/src/capability/` — the TypeScript surface future capabilities are authored against. Scope rulings bind this surface: NO capability ships (loader table EMPTY — the co-located hermetic fixture suites are the day-one named consumers), ZERO new CLI surface (no flags/env vars/verbs; bare `pio` stays the reserved stub), output validation deliberately ABSENT (deferred to a follow-up goal after the first real capability — both considered variants recorded in the goal's PLAN Notes), and agent-side completion surfaces dropped (S3): one `execute_phase` iteration is one observed agent RUN and **run-ended is the completion event**, so all phase-stopping rides run-boundary observation.
- **Contracts (`contract.ts`):** v1 `Contract {name, version, inputs, outputs, writes, allowProjectWrites?}` — NO `sandbox` field (struck per S4; the spec's `SandboxProfile` sketch collides with R2's rendered-side type of the same name — the owning step re-introduces it non-breakingly against the delivered R2 vocabulary). IO slots are a union of file-backed `MarkdownFileSpec {name, file?, paramKey?}` and plain-value `ValueSpec {name}` — name-only entries are VALID value slots (owner ruling superseding the plan AC row). `classifySpec` is the ONLY shared resolution surface (4 modes: `value`/`file`/`missing-value`/`unresolvable`; pure; raw relative paths; a non-empty-string `paramKey` value BEATS static `file` — deliberate divergence from the legacy root ordering; do NOT copy the legacy ordering into `pio/`). Input validation is AUTOMATIC pre-spawn (`validateInputs`: collect-all in declaration order, ONE `ContractViolationError` throw, silent pass; value slots never touch the FS); output validation is author-opt-in and lands in the follow-up goal. `checkContract` (non-throwing `{ok} | {ok, problems[]}`) feeds the loader's refusal lines.
- **PioSession host (`pio-session.ts`):** static async `create(cwd, sessionsRoot?)` + private constructor — capabilities receive READY instances by reference and never construct sessions themselves. ONE instance-scoped `subscribe` listener feeds the counters store: `toolUses` (per tool name, counted at `tool_execution_start`, failures included) · `filesWritten` (committed at successful `tool_execution_end` — the END event carries NO `args`, so the path correlates from the START via `toolCallId`; pending entries drained at `agent_start`) · `askUserCalls` (name-matches `"ask_user"`; structurally 0 until the dist hosts such a tool) · `tokens` (assistant `message_end` usages ONLY — `turn_end` double-counts; LOCAL five-field accumulator because `addUsageToTotals` is NOT root-exported by the installed dist — do NOT substitute `getSessionStats()`, wrong aggregation channel). `execute_phase(id, opts)` closed option bag `instructions?`·`min?`·`max?`·`shouldStopLoop?`: `min` floor / `max` ceiling (breach ⇒ `PhaseBudgetError` — its only budget throw site), `shouldStopLoop?: (ctx) => Promise<boolean>` judged BETWEEN runs (true ends the phase, false demands another; omission-loud — a bare `async () => {}` no longer satisfies the contract). Result EXACTLY `{done, iterations, messages, varsDelta, counters, tokens}`. Read/reset window pairs (`getFilesWrittenDelta`/`resetFilesWrittenDelta`, `getRunMessages`/`resetRunMessages`) are non-consuming baseline slices with manual resets owned by the engine — cumulative counters stay append-only.
- **Phase markers (`renderPhaseMarker`, sole routine):** `── <id> ──` (U+2014 em-dash ×2, composed from `\u2014` escapes so the pinned bytes survive editor/toolkit mangling) renders as the LEADING LINE of the instructions sent with each phase run — a transcript-visible segment stamp read by people/auditors scrolling the audit trail; re-runs of a phase re-stamp the same line (framing carried over — the persistent transcript already carries prior runs). NO capability kickoff line (owner removal mid-step; negative pins assert composed prompts carry only the engine marker + authored instructions). Phase `id` is a call-site label, not a lookup key.
- **PioCapability base (`base.ts`):** `declare readonly contract` (authoring obligation on the subclass), abstract `call(inputs)`, and base-provided NEVER-OVERRIDDEN `run(inputs?)` — THE composition seam: pre-spawn `validateInputs` (this goal's only `ContractViolationError` throw site) → `call()` → `SessionStatus` assembly; ONE catch-all spans both steps (uniform failing payload, single-element JSON-safe `errors[]`); `run()` never rejects, keeping the process boundary linear. Completion invariant: returning from `call()` completes — there is NO completion method; workflow flags (COMPLETED/APPROVED/BLOCKED…) are authored in capability code, the runtime synthesizes no state it didn't observe.
- **Error home (`errors.ts`):** four-export surface — `CapabilityErrorCause` (closed five-member union: budget/kill/spawn/contract/author-halt), standalone `ContractViolationError extends Error` (violations-first, auto `cause: "contract"` — self-describing cause fields survive serialization where `instanceof` does not), `PhaseBudgetError {iterations}`, `SessionStatusError` (JSON-safe `errors[]` element shape). Zero imports. The spec's `CapabilityError{partial?, cause}` envelope was struck for lacking a v1 consumer (re-add trigger lives in the goal's S01 docs); `SessionStatus` is declared at `status.ts` only.
- **Loader (`loader.ts`):** capabilities-only built-in loading — hardcoded static name→factory table (ships EMPTY; probe is NEVER in it), five-stage resolution (exact case-sensitive `Object.hasOwn` lookup → fire only the named factory → reference-equal prototype-chain IDENTITY walk vs the bundled `PioCapability` [two-runtime-copy integrity guard] → throwaway-construction contract introspection → load-time `checkContract`), single-owned refusal lines for all outcome families (successor of `CAPABILITY_NOT_IMPLEMENTED` — deleted from `sandbox/run.ts`), never-rejects discriminated results, lazy-import discipline preserved. `--cap` flag, `~/.pi/capabilities/` lane, resolution-order policy, full discovery, and `list-caps` remain frozen non-goals. Adding a real capability = literal table entry + its module + lockstep update of the loader suite's dynamic-specifier source-guard row.
- **Entry-arm delegation (D6):** the literal `probe` arms stay BYTE-STABLE in all three entry points (the R2 launch battery keeps `pio run probe` as its stable target; probe retirement remains a separate coordinated change with the battery). EVERY non-probe name resolves through the loader BEFORE any engagement/TTY/nesting/bwrap side effect (cheap pre-launch refusal, exit 1, zero side effects — mechanically asserted), and the in-namespace top entry drives the full pipeline: resolve → fresh `PioSession.create(cwd, sessionsRoot)` → instantiate with `{session}` → base `run()` → REAL status-emitter construction (`armKillCapture()` armed before `run()`) → `emit` → mapped exit code, wrapped by its last-resort boundary so record + exit code land on ALL exit paths (pre-emitter faults degrade plain). Observable at delivery: `pio run <anything-but-probe>` prints the loader's refusal line pre-launch. Inherited legs (real SIGTERM through a live session mid-phase; real `pio run <capability>` over pty) ride the FIRST-REAL-CAPABILITY goal per scope ruling S2.
24. **Terminal status record (`status.json` / `SessionStatus`, goal `capability-base`):** the capability-path process writes EXACTLY ONCE at process exit, never updated — mid-run progress lives in the transcript; each new `pio run` mints a fresh engagement id, so prior records accumulate as an untouchable audit trail (plain re-entry is re-execution, not restoration). Placement `<engagementDir>/.sessions/top/status.json` (beside the top transcript — spec §7 placement governs). Schema: `ok` · `capability {name, version, source: "builtin"}` · `outputs` (the value object `call()` returned) · `errors?` (JSON-safe typed captures `{type, cause?, message?, violations?}`, absent when ok) · `transcriptRef?` (path RELATIVE to the engagement dir — survives repo moves; the transcript filename is DYNAMIC in the installed dist, `<timestamp>_<sessionId>.jsonl`, computed at emission from the live session-file accessor) · `tokens` (reuses the `PioSession` counter scalar VERBATIM) · `durationMs` (run-start → emission); nullish omitted on serialization (canonical key order, 2-space + trailing newline). Single-emission guard: completion OR the kill handler claims first, losers delegate to the winner — one write per process, the file is never updated. Exit codes: `ok:true`→0 · any typed failure (contract/budget/author-halt/unknown)→1 · user-interrupt 130 stays owned by pi's native Ctrl-C×2 coalescing (kept, not re-implemented) · SIGTERM → bounded grace (`DEFAULT_KILL_GRACE_MS` 250 ms), live run cancelled/awaited best-effort, partial `{ok:false, errors:[{cause:"kill"}]}` then exit 1 — the entry-side handler must WIN over pi `InteractiveMode`'s prepended SIGTERM→exit(0) handler (ordering pinned with an injected signal seam). `probe` produces NO `status.json` (byte-stable path bypasses the authoring layer). `SessionStatus` is declared at `capability/status.ts` ONLY — never re-declared elsewhere.

## Service Integrations

### pi Framework Integration

pio depends entirely on the pi coding agent framework (`@earendil-works/pi-coding-agent`). Key integration points:
- **ExtensionAPI:** `registerTool()`, `registerCommand()`, `on(event)`, `setModel()`
- **Session management:** `ctx.newSession()`, `ctx.sessionManager.getEntries()`, custom entries
- **Event system:** `resources_discover`, `before_agent_start`, `turn_end`, `tool_call`, `turn_start`
- **UI notifications:** `ctx.ui.notify()`
- **Message delivery:** `pi.sendUserMessage()`

### pi SDK Integration (standalone pio executable)

The `pio/` package drives pi agent sessions programmatically through the pi SDK (exact-pinned 0.85.1 in `pio/node_modules`), complementing the event-driven ExtensionAPI integration above:

- **Entry chain (post-R2):** `pio/bin/pio` (thin JS delegator — one dynamic import + exit-code sink, natural event-loop drain, no `process.exit`) → `pio/src/cli.ts` (strict parser + dispatch; reaches the SDK only via the lazy literal `import("./sandbox/run.ts")`, so `--help`/error paths never pay SDK startup cost). Host grammar = the R1 forms with the run path unified behind that single hoisted import — the literal `probe` fast-case stays byte-identical and every other name delegates onward as `runCapability(name, out)` so the loader gate decides (cli.ts owns zero refusal bytes; help text carries the advisory line that no capabilities are resolvable yet) — the transient `session-run` verb shipped mid-R2 was REMOVED by net-shrink (host-facing surface strictly smaller than its pushed tip; the discarded host-refusal gate is permanently moot; removed surfaces return only via a new explicit owner decision). Builtin contract: targets export `run(io?, opts?): Promise<number>` where the resolved value IS the process exit code; `main()` and the bins are the sole exit-code sinks.
- **Sandboxed launch path (R2 — host-side only):** `runCapability` (`src/sandbox/run.ts`) runs the strict gate sequence (capability → TTY fast-fail → anti-nesting: `PI_SANDBOX === "1"` in the host launcher's OWN env ⇒ typed refusal → constructive bwrap pre-flight judged by construction, not binary presence alone), then — side effects reached ONLY on the proceeding path — ensures the engagement layout and the `<stateRoot>/.pi` tree (mkdir-only, first use; creation precedes rendering), renders ONE self-contained profile, builds the complete bwrap argv (pure function), writes the retained `profile.json`, prints the mount list to stderr pre-spawn, and supervises the spawn (stdio inherit — no pty allocated anywhere; host SIGTERM forwarded + awaited; exit map 0→0 / 130→130 / other→1; natural drain). One namespace per top-level run; the `PI_SANDBOX=1` env-out marks the bubble (anti-nesting signal + detectability). Every refusal is ONE physical line to stderr + nonzero exit + zero side effects, naming the failing layer.
- **In-namespace top process (R2):** the launched target vector is `bin/pio-run-session <capability> --sessions-root <engagement>/.sessions` (byte-frozen form; the S05 producer/consumer pair) — the artifact's OWN dedicated entry: `bin/pio-run-session` (thin ESM delegator, mode 100755, NOT registered in `package.json` bin — invoked by absolute path from inside the bubble) + `src/run-session.ts::runSession` (strict two-value SYNTACTIC-ONLY parse with byte-pinned error families; capability gate BEFORE TTY preflight; probe arm keeps DIRECT dispatch — byte-stable, and deliberate: the in-namespace process carries `PI_SANDBOX=1` in its OWN env, so routing it through the run path would trip anti-nesting; the non-probe arm, added by goal `capability-base`, drives the full pipeline — loader resolve → `PioSession.create(cwd, sessionsRoot)` → instantiate with `{session}` → base `run()` → REAL status-emitter construction + `armKillCapture()` armed before `run()` → `emit` → mapped exit code — under its own last-resort boundary (best-effort captured record post-emitter; pre-emitter faults degrade plain); ZERO static VALUE imports — pure-type clauses erase under erasable syntax; dynamic literal thunks `./probe.ts`, `./capability/loader.ts`, `./capability/pio-session.ts`, `./capability/status.ts`, keeping the lazy-SDK discipline mechanically provable). Only argv bytes cross the namespace boundary (argv is the channel carrying `{capability, sessionsRoot}`); diagnostics originating there carry a self-identifying `pio-run-session: ` prefix while single-source refusal lines emit unprefixed and byte-identical across both entry points. Retirement posture (REVISED by goal `capability-base`): R2's contiguous-deletion framing no longer holds — `session.ts` was GENERALIZED, not forked, to host the capability path (`CreateProbeSessionOptions`), and the run-session v1 body now implements the real non-probe pipeline. What remains on the retire list is `probe.ts` itself — still a separate coordinated change with the R2 launch battery.
- **Session wiring (durable R1 asset; template for later capability bases):** mirrors pi's own CLI seam verbatim against the pinned dist — `SessionManager.create(cwd[, sessionDir])` (the second arg is THE R2 session delta: in-namespace launches pass the engagement's `.sessions/top/` directory so transcripts land in the already-rw engagement slot; standard JSONL, openable afterwards by a normal host `pi` TUI — interop preserved) → stored `CreateAgentSessionRuntimeFactory` closure (re-invoked by `/new`/`/resume`/`/fork`/import flows — forwarding `sessionStartEvent` into `createAgentSessionFromServices` is what makes that reuse work) → `createAgentSessionServices({ cwd, agentDir })` with **default resource discovery** (no overrides/extensions filtering) → `createAgentSessionFromServices({ services, sessionManager, sessionStartEvent })` with model/thinking/tools omitted (disk-backed defaults; no hardcoded models — agentDir flows from `getAgentDir()`, which honors `PI_CODING_AGENT_DIR`, so inside the bubble every default resolves from the pio-owned `<stateRoot>/.pi/agent` tree rather than host `~/.pi`) → returns `{ ...created, services, diagnostics }` (the missing `+services/+diagnostics` extension is the measured 0.74→0.85 drift trap) → `createAgentSessionRuntime(factory, { cwd, agentDir: getAgentDir(), sessionManager })`. Generalized (not forked) by goal `capability-base` with an optional third parameter `CreateProbeSessionOptions {sessionListener?, customTools?}`: the one-time `subscribe` attach sits OUTSIDE the stored factory closure right after construction settles (exactly-once-at-construction falls out of the single call site; re-created sessions — `/new`·`/resume`·`/fork` — get no fresh attach: inert in v1, one session per engagement; the returned unsubscribe is deliberately discarded), and `customTools` threads identity-only when provided (key-absent default — reserved slot, v1 registers none).
- **TUI hosting:** `new InteractiveMode(runtime, { initialMessage })` over inherited stdio — raw mode, rendering, resize, and Ctrl-C coalescing are pi-native (double Ctrl-C within the 500 ms window quits cleanly; native shutdown owns terminal restore + `runtime.dispose()` + exit 0). Probe failure path: best-effort `im.stop()` then `runtime.dispose()`, readable cause on stderr, resolve 1.
- **Physical-copy finding (2026-09-16, quality gate):** the probe-loaded pio extension bound exclusively to the pinned 0.85.1 runtime — `src/index.ts` imports the SDK type-only (`import type { ExtensionAPI }`, erased at load), so the root's devDependency copy was never loaded into the process; mixed-copy misbehavior: none observed. Documented last-resort escape hatch if the mix ever misbehaves: filter the extension out of probe discovery via the public `extensionsOverride` seam (explicit user approval required; neither needed nor applied).

### Filesystem as State Store

All workflow state is stored in the `.pio/` directory tree:
- **`.pio/goals/<name>/`** — goal workspaces with GOAL.md, PLAN.md, `PLAN_ARCHIVE/` (timestamped archived plans), step folders (S01/, S02/), and optional nested subgoal workspaces under `S{NN}/subgoals/<name>/`
- **`.pio/issues/`** — issue backlog as markdown files
- **`.pio/session-queue/task-{key}.json`** — per-goal task queue slots (key is goal basename for flat goals, hierarchical `parent__S03__nested` for subgoals)
- **`.pio/PROJECT/`** — 7-file project context (OVERVIEW.md, DEVELOPMENT.md, etc.)

The global pio workspace at `~/.pi/pio/` (configurable via `~/.pi/pio-config.yaml` → `workspace.dir`) stores shared runtime state:
- **`~/.pi/pio/state/<sessionId>.json`** — per-session loop engine state (`currentPhase`, `currentIteration`, `isAdHocInput`). One JSON file per active session, keyed by `ctx.sessionManager.getSessionId()`.

The standalone `pio` executable (R2) additionally binds a separate GLOBAL ENGAGEMENT STATE ROOT outside the repo — do not conflate with either of the above:
- **`~/.pio/projects/<projectKey>/engagements/<engagementId>/`** (stateRoot = `~/.pio`, overridable via `PIO_STATE_DIR`) — per-engagement audit trail for each top-level `pio run`: rendered-and-retained `profile.json`, transcripts under `.sessions/top/` (later child sessions nest under `.sessions/<childId>/`), outcomes. Created on first use; never deleted post-finish
- **`~/.pio/.pi/`** — the pio-owned pi configuration tree (whole tree rw; the vehicle's isolated pi instance relocated there via `PI_CODING_AGENT_DIR`; created on first use by mkdir-only — pristine start, zero seeding). Inside the bubble the state root is mounted RO globally (sibling engagements readable), with the own project slot and the `.pi` tree overlaid RW; host `~/.pi` is unbound entirely

### External Model Configuration

Optional `~/.pi/pio-config.yaml` allows per-capability model overrides and workspace configuration. Resolution order for models: capability-specific → default → inherit parent model. Parsed at runtime by `model-config.ts`.

- **`workspace.dir`** (optional, string) — Custom pio workspace directory path. Defaults to `~/.pi/pio`. Controls where per-session loop engine state files are stored (`state/<sessionId>.json`).

### Jira Integration (Skill-Only)

Jira operations are handled entirely through the `pio-jira` skill (`src/skills/pio-jira/`) — no TypeScript capability code exists for Jira. Agents invoke the Atlassian CLI (`acli`) via `bash` tool calls, guided by protocol instructions in SKILL.md and REFERENCE.md.

**Config file:** `.pio/jira-config.yaml` (optional) — stores `site`, `projectKey`, and `defaultType`. Created by `src/skills/pio-jira/scripts/setup-config.sh` (POSIX shell, invoked via `bash` tool). Triggered before any Jira operation when the config is missing.

**Workflow:** Jira ticket → pull to local issue (`pio_create_issue`) → create goal from issue (`pio_goal_from_issue`) → full pio lifecycle.

**Skill-only rationale:** Avoids TypeScript capability overhead for CLI-driven workflows. The skill delegates all command construction and error handling to agents at runtime.
