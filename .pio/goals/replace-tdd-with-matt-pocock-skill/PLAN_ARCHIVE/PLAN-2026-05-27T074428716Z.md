---
totalSteps: 3
steps:
  - name: rename-all-references
    complexity: task
  - name: refactor-execute-task-prompt
    complexity: task
  - name: delete-old-skill-and-verify
    complexity: task
---

# Plan: replace-tdd-with-matt-pocock-skill

Replace the `test-driven-development` skill with Matt Pocock's `tdd` skill by updating all code references, then deleting the old skill directory. The new skill is already copied to `src/skills/tdd/`.

## Prerequisites

None. The `src/skills/tdd/` directory already exists with all 6 files from Matt Pocock's repository (verified identical via `diff -r`).

## Steps

### Step 1: Rename all references to the new skill name

Rename `test-driven-development` to `tdd` in all capability config `mandatory` arrays, prompt examples, and test data. Pure string replacements — no content changes.

**Acceptance Criteria**

- `grep -rn "test-driven-development" src/ --include="*.ts" --include="*.md"` returns only the old skill directory itself (`src/skills/test-driven-development/SKILL.md`)
- `npm run check` reports no errors
- Full test suite passes: `npx vitest run` exits with code 0

**Files Affected**

- `src/capabilities/execute-task.ts` — `"test-driven-development"` → `"tdd"` in `skills.mandatory`
- `src/capabilities/review-task.ts` — same rename
- `src/capabilities/execute-plan.ts` — same rename
- `src/capabilities/test-skills-cap.ts` — same rename
- `src/prompts/evolve-plan.md` — same rename in examples (2 occurrences)
- `src/index.test.ts` — skill discovery assertion: `"test-driven-development"` → `"tdd"`
- `src/frontmatter-schemas.test.ts` — test data frontmatter: same rename (2 occurrences)
- `src/types.test.ts` — schema validation test data: same rename (3 occurrences)
- `src/capability-config.test.ts` — expected skill name assertion: same rename
- `src/fs-utils.test.ts` — base capability skills array: same rename

### Step 2: Refactor execute-task prompt for WHAT/HOW split

Refactor `execute-task.md` to respect the core principle: prompts describe WHAT, skills describe HOW. Currently Step 5 prescribes specific TDD patterns (RED→GREEN→REFACTOR cycle, Arrange-Act-Assert, DAMP over DRY, one assertion per concept). Replace this with a reference to the mandatory `tdd` skill — e.g., "Follow the mandatory `tdd` skill for test structure guidance." This keeps the prompt stable when skill content changes.

**Acceptance Criteria**

- `src/prompts/execute-task.md` no longer lists specific TDD methodology patterns (RED→GREEN, Arrange-Act-Assert, DAMP over DRY)
- The prompt references the mandatory `tdd` skill as the source of TDD guidance
- Prompt still describes what to do: write tests first, derive from acceptance criteria, verify tests fail initially
- `npm run check` reports no errors

**Files Affected**

- `src/prompts/execute-task.md` — refactor Step 5 ("Write tests first") to reference the `tdd` skill instead of prescribing specific patterns

### Step 3: Delete old skill and verify

Remove the `src/skills/test-driven-development/` directory. After deletion, only `tdd` should remain as the TDD skill. Verify both type checking and test suite pass with the final state.

**Acceptance Criteria**

- `src/skills/test-driven-development/` directory no longer exists
- `grep -rn "test-driven-development" src/ --include="*.ts" --include="*.md"` returns zero results
- `npm run check` reports no errors
- Full test suite passes: `npx vitest run` exits with code 0

**Files Affected**

- `src/skills/test-driven-development/` — delete entire directory including SKILL.md

## Notes

- Pocock's `tdd` skill uses progressive disclosure via reference files (`tests.md`, `mocking.md`, `interface-design.md`, `deep-modules.md`, `refactoring.md`) linked from `SKILL.md`. Pi's skill discovery reads `SKILL.md` and resolves relative links — the multi-file structure is compatible.
- Skill discovery is filesystem-based (scan `src/skills/` for dirs containing `SKILL.md`). Currently 9 skills are discovered including both `tdd` and `test-driven-development`. After deletion, 8 remain — `tdd` replaces `test-driven-development`.
- The skill name in the YAML frontmatter (`name: tdd`) determines how pi's `<available_skills>` block labels the skill. Runtime references (capability `mandatory` arrays, prompt mentions) must match this name exactly.

