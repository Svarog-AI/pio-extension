# Tests: Skill injection logic — review re-execution (Step 3)

Addresses review feedback: extracts duplicate helpers, replaces fragile spy test, cleans up dead comments. The implementation (`buildSkillLoadingSection`, `before_agent_start`, `resources_discover`) is unchanged — only test quality issues are fixed.

## Unit Tests

### Test file structure (no behavioral changes)

Given `makeSkill()` and `writeSkillFile()` helpers were duplicated across two describe blocks when extracted to a shared location then all tests still use the shared helpers.
Given the abandoned section headers at lines 42-45 when cleaned up then the test file contains no dead comments.
Given the fragile `readFileSync` spy test for `_skill-loading.md` when replaced with a positive behavior test then the test verifies skill content comes from `buildSkillLoadingSection` rather than spying on fs reads.

### buildSkillLoadingSection (existing tests, unchanged behavior)

Given a config with no skills and an empty registry when buildSkillLoadingSection is called then it attempts global mandatory skills and returns undefined when none resolve.
Given a config with mandatory skills and matching registry entries when buildSkillLoadingSection is called then mandatory skills are wrapped in XML tags.
Given a config with recommended skills when buildSkillLoadingSection is called then recommended skills appear as instruction-based listings.
Given a config with both mandatory and recommended skills when buildSkillLoadingSection is called then the output contains both sections.
Given a mandatory skill whose file does not exist on disk when buildSkillLoadingSection is called then it logs a warning and skips.
Given a mandatory skill whose name is not in the registry when buildSkillLoadingSection is called then it logs a warning and skips.
Given global mandatory skills that overlap with config mandatory skills when buildSkillLoadingSection is called then duplicates are deduplicated.
Given a skill with YAML frontmatter in SKILL.md when buildSkillLoadingSection reads and strips it then the injected body does not contain frontmatter delimiters.
Given a config with undefined skills field when buildSkillLoadingSection is called then it does not crash and returns undefined when no skills in registry.

### before_agent_start integration (existing tests, unchanged behavior)

Given before_agent_start with mandatory skills when the handler runs then the message contains SKILL LOADING INSTRUCTIONS with injected blocks.
Given before_agent_start when the handler runs then delivery order is PROJECT OVERVIEW, then SKILL LOADING INSTRUCTIONS, then YOUR INSTRUCTIONS.
Given the skill registry is populated via systemPromptOptions.skills when before_agent_start runs then the registry is cached.

### resources_discover (replaced fragile spy test with positive test)

Given resources_discover is triggered when the handler runs then skill loading content is generated by buildSkillLoadingSection (not from a static file).

## Programmatic Verification

Given the TypeScript project when npx tsc --noEmit is run then it exits with code 0.
Given the test suite when npm test is run then all tests pass with no regressions.
